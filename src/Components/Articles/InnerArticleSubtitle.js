import React from 'react';

const InnerSubtitle = ({innerSubtitle}) => {
    return (
        <div>
                <h3 className="innerArticleSubtitle">{innerSubtitle}</h3>
        </div>
    );
};

export default InnerSubtitle;
