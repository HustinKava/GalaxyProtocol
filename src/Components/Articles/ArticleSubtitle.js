import React from 'react';

const Subtitle = ({subtitle}) => {
    return (
        <div>
                <h2 className="articleSubtitle">{subtitle}</h2>
        </div>
    );
};

export default Subtitle;
