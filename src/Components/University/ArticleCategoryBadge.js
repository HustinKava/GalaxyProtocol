import React from 'react';

function CategoryBadge({ categoryType }) {
    return (
        <>
            <p className="category-badge">{categoryType}</p>
        </>
    )
} 

export default CategoryBadge;
